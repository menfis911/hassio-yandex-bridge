# Yandex HA Bridge

Кастомная интеграция для Home Assistant, которая получает устройства Яндекса через официальный API Яндекса и добавляет выбранные устройства непосредственно в Home Assistant.

## Текущая версия

**0.3.6 — исправление диагностики управления и OAuth permissions**

Интеграция работает через Config Flow: OAuth-токен → получение списка устройств → выбор устройств → создание Config Entry в Home Assistant.

Используется уникальный домен `yandex_ha_bridge`, поэтому интеграция может работать одновременно со сторонней интеграцией `yandex_smart_home`.

## Что исправлено в 0.3.6

- Обработка `HTTP 403` от Yandex API теперь возвращает понятную причину вместо безымянного `HTTP 403`.
- В ошибку добавляется сообщение Yandex API и `request_id`, если Яндекс его вернул.
- Для операций управления явно проверяется требование OAuth scope `iot:control`.
- Добавлен `User-Agent` интеграции.
- GitHub Actions дополнительно проверяет актуальную версию README и CHANGELOG.
- Текущая версия синхронизирована во всех основных компонентах проекта: **0.3.6**.

### Важное про HTTP 403

Для чтения списка и состояния устройств достаточно OAuth scope `iot:view`. Для `POST /v1.0/devices/actions`, который используется для включения, выключения, яркости и цвета, Яндекс требует **`iot:control`**. Это официальное требование Yandex Smart Home API.

Если интеграция получает `HTTP 403` при любом действии, необходимо использовать OAuth-токен, выданный с правами **`iot:view iot:control`**. Само наличие устройств в списке не означает наличие права управления: чтение и управление имеют разные OAuth scopes.

## Возможности 0.3.x

- OAuth-токен Яндекса.
- Получение списка устройств через официальный API.
- Выбор устройств при установке.
- Повторное изменение списка устройств через Options Flow.
- Регистрация выбранных устройств в Home Assistant.
- Управление устройствами типа `light`.
- Включение и выключение.
- Яркость.
- RGB/HSV.
- Цветовая температура.
- Периодическое получение состояния.
- Device Registry Home Assistant.

## Как это работает

```text
        Яндекс Умный дом
               │
               │ официальный API
               ▼
      Yandex HA Bridge API
               │
               ▼
       Home Assistant
               │
               ▼
        Config Entry
               │
               ▼
       выбранные устройства
```

## Установка

Репозиторий устанавливается через HACS как custom integration.

После установки:

1. Откройте **Настройки → Устройства и службы**.
2. Нажмите **Добавить интеграцию**.
3. Найдите **Yandex HA Bridge**.
4. Введите OAuth-токен Яндекса с правами **`iot:view` и `iot:control`**.
5. Получите список устройств.
6. На первом тесте выберите **одно устройство**.
7. Сохраните Config Entry.
8. Перезапустите Home Assistant, если HACS попросит это сделать.

Не удаляйте существующую Config Entry без необходимости. Для изменения выбранных устройств используйте **Настроить / Options**.

## Важно

`Yandex HA Bridge` — отдельная интеграция с доменом `yandex_ha_bridge`. Она не требует удаления или изменения сторонней интеграции `Yandex Smart Home`.

Веб-панель App предназначена для диагностики и технического обслуживания. Основное создание сущностей выполняется custom integration через Config Flow.

## Поддержка света

Для совместимых устройств используются capabilities Яндекса:

- `devices.capabilities.on_off`
- `devices.capabilities.range` / `brightness`
- `devices.capabilities.color_setting` / `rgb`
- `devices.capabilities.color_setting` / `hsv`
- `devices.capabilities.color_setting` / `temperature_k`

Яркость преобразуется между шкалами Home Assistant `0–255` и Яндекс `0–100`.

## Версии

### 0.3.6
- Исправлена диагностика `HTTP 403` при управлении.
- В API-ошибки добавляются подробности ответа Яндекса и `request_id`.
- Явно документировано требование `iot:control` для управления.
- Добавлен `User-Agent`.
- Усилены CI-проверки актуальной документации и версии.

### 0.3.5
- Исправлен импорт `ATTR_BRIGHTNESS`: актуальный Home Assistant предоставляет его из `homeassistant.components.light`.
- Исправлена загрузка платформы `light`.
- Добавлена автоматическая CI-проверка этого критичного импорта.

### 0.3.4
- Исправлен запуск `DataUpdateCoordinator`: убрана зависимость от `hass.logger`.
- Синхронизированы версии App и custom integration.
- Обновлены CI и полная документация.

### 0.3.3
- Исправлена структура переводов custom integration.
- Добавлены `translations/en.json` и `translations/ru.json`.
- Удалён устаревший `strings.json`.

### 0.3.2
- Синхронизированы версии App, custom integration и исходного кода.
- Удалены устаревшие ссылки на 0.3.0.
- Удалена устаревшая integration из `custom_components/yandex`.

### 0.3.1
- Интеграция переименована в **Yandex HA Bridge**.
- Используется домен `yandex_ha_bridge`.
- Добавлены Config Flow и Options Flow.
- Добавлена поддержка света, яркости, RGB/HSV и цветовой температуры.

### 0.3.0
- Добавлена стандартная Home Assistant custom integration.
- Config Flow: токен → устройства → выбор.
- Config Entry и Options Flow.
- Добавлена сущность `light`.

### 0.2.x
- Базовое управление устройствами.

### 0.1.x
- Авторизация.
- Обнаружение устройств.
- Чтение состояния.

## Разработка и релизы

Версия должна быть синхронизирована минимум в следующих местах:

- `custom_components/yandex_ha_bridge/manifest.json`
- `yandex_ha_bridge/config.yaml`
- `yandex_ha_bridge/app/bridge.py`
- `yandex_ha_bridge/CHANGELOG.md`
- `README.md`

GitHub Actions выполняет validation, проверяет актуальный Home Assistant API, собирает `amd64` и `aarch64`, публикует multi-arch manifest и после успешной сборки создаёт соответствующие tag и GitHub Release.
